<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Painted behind the Flutter UI, including for the split second before
         it draws its first frame. Matches AppPalette.light.bg in
         lib/theme.dart; values-night/ carries the dark counterpart, so the
         launch flash follows the system setting instead of always being pale. -->
    <color name="launch_background">#F2F2F7</color>
</resources>
